<!DOCTYPE html>
<html>
  <head>
    <title>Instabooks</title>
    <link rel="shortcut icon" href="/assets/favicon.ico" type="image/x-icon">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= yield :head %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield :facebook_meta_tags %>
  </head>
  <body class='user <%= @body_class %>'>
    <header>
      <div class='container'>
        <hgroup class='logo'>
          <h1><%= link_to 'Instabooks', albums_path %></h1>
        </hgroup>

        <% if current_user %>
          <div class='user-info'>
            <div class='actions dropdown'>
              <button class='button'>Menu</button>
              <ul>
                <li><%= link_to_modal "Create album", new_album_path %></li>
                <li class='last'><%= link_to "Sign out", signout_path %></li>
              </ul>
            </div>
          </div>
        <% else %>
          <div class='unauthenticated'>
            <p>Create your first album now!</p>
            <%= link_to "Connect your Instagram account", "/auth/instagram", class: 'button connect-header' %>
          </div>
        <% end %>
      </div>
    </header>

    <div class='container'>
      <%= yield %>
    </div>

    <footer>
      <div class='container'>
        Created by <a href='http://github.com/bchagas'>Bruno Chagas</a>. Questions? Tweet <a href='http://twitter.com/instabooksme'>@instabooksme</a>
      </div>
    </footer>
    <%= yield :javascripts %>
    <%= render 'partials/analytics' %>
  </body>
</html>
